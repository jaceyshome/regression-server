{
  "apps": [{
    "name": "",
    "script": "web",
    "instances": "max",
    "exec_mode": "cluster",
    "watch": ["web", "node_modules", ".env", "package.json"],
    "env": {
      "NODE_ENV": "development"
    }
  }]
}
